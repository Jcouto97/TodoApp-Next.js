import {createContext} from 'react';

/* interface AppContextInterface{
    completed: string[],
    setCompleted(): void 
}
 */

// AppContextInterface | null ???????

export const CompletedContext = createContext<any>(null)